# 更新日志

本项目使用语义化版本号。面向用户的更新说明以中文为主。

## v2.0.0

发布日期：2026-09-25

### 新增特性

- **多标签页工作台**：支持多文件同时打开与拖拽，独立管理每个标签的编辑状态、未保存标记与校验结果。
- **15+ 种配置格式支持**：全面覆盖 JSON、JSONC、JSON5、YAML、TOML、XML、CSV、TSV、INI、ENV、Properties、EditorConfig、GitConfig、HCL/Terraform、JSONL。
- **多模态编辑模式**：
  - **代码源码视图**：集成实时语法纠错、行号槽、光标行列位置指示与快捷键保存（`Ctrl+S`）。
  - **可视化树形视图**：基于统一 AST 架构，直观浏览与增删改节点结构。
  - **Schema 动态表单**：根据配置 Schema 规范自动渲染交互式填写表单。
  - **双模差异对比**：同时支持行级文本 Diff（LCS）与 AST 语义 Diff（路径与字段对比）。
- **智能格式嗅探与排版**：全面打通格式适配器注册表，支持全格式自动特征嗅探与一键标准缩进排版。
- **Profile 规范诊断**：内置 `package.json`、`docker-compose.yml` 等场景规范检查，辅助排查必填项与配置有效性。
- **预置示例模板**：内置各类配置格式的开箱即用示例，提供功能指引与快速上手体验。

### 缺陷修复与性能加固

- **Diff 对比白屏**：重构 LCS 行索引匹配器（`computeLCSMatchSets`），彻底解决差异对比视图渲染异常。
- **拖拽解析兼容**：适配后端元组数组结构，消除文件拖拽至应用时的未定义异常。
- **IPC 契约对齐**：重构 `cmd_save_file`、`cmd_detect_profile`、`cmd_validate_profile` 入参容错逻辑，确保前后端通信一致。
- **XML 兄弟节点路径**：引入索引隔离与属性收集机制，解决 XML 同名标签删除/修改补丁定位失效问题。
- **自动化测试加固**：全面补齐前端契约测试与后端弹性测试，测试套件扩充至 146 项并通过。

## v1.1.0

发布日期：2026-09-03

### 品牌升级

- 项目与桌面应用正式更名为 Orthos。
- npm 包、Rust crate、应用标题、图标资源和 Windows 发布包统一使用新名称。
- README 与项目介绍重写，直接说明支持格式、安全边界、私隐和下载方式。
- 保留 `com.lintdrop.desktop` 应用标识符，延续旧版安装与升级识别。

### 缺陷修复与安全加固

- **前端安全**：重构 `escapeHtml` 函数，补充对双引号与单引号的完整实体转义，消除 HTML 属性注入漏洞。
- **CSV 规范支持**：重构分词器以支持 RFC 4180 跨行字段，正确解析引号内包含真实换行符的 CSV 文件，避免误判为列数不一致。
- **XML 属性保护**：属性扫描与标签栈修复增加引号状态跟踪，包含 `>` 等比较符号的合法属性不再被提前截断。
- **启发式识别修正**：优化 INI / TOML / ENV 格式特征嗅探顺序，带 `#` 注释头或裸值的 INI 文件能被精准识别。
- **ENV export 支持**：识别并保留 POSIX/Docker 风格的 `export ` 前缀，清洗键名非法字符时不破坏合法空格。
- **INI 跨块重复键**：重构键名跟踪为按 Section 分组存储，支持分散声明的同名 Section 之间的重复键检测。

## v1.0.4

发布日期：2026-08-22

### 安全修复

- YAML、TOML、INI 的重复键不再被自动删除，避免静默丢失配置。
- CSV 多余字段不再被截断，交由用户确认处理。
- 自动修正内容必须再次通过对应格式校验，才会提供保存入口。
- JSON Schema 禁用外部网络引用，保持离线检查。

### 性能与质量

- 大文件 Diff 改用有界算法，并增加渲染行数上限。
- JSON Schema 支持一次返回全部校验问题。
- 新增前端回归测试、解析器语料测试和 Rust 基准。
- 新增 Windows GitHub Actions 质量门禁。

### 中文体验

- 应用主页、操作按钮、状态提示和关于页面改为中文优先。
- README 重构为中文主文档，英文保留简短介绍。
- 新增中文贡献指南，明确开源参与流程。

## v1.0.3

- 新增 Windows 10/11 免安装版。
- 保留 Windows 安装版并修复快捷方式图标绑定。
- 完善应用、安装器和卸载器图标。
