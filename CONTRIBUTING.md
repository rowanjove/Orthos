# 参与贡献

感谢你为 LintDrop 提交改进。项目以中文作为主要沟通语言，同时欢迎英文 Issue 和 Pull Request。

## 提交问题

提交 Issue 前，请先搜索是否已有相同问题。Bug 报告建议包含：

- LintDrop 版本和 Windows 版本。
- 输入文件的格式，以及能够复现问题的最小示例。
- 实际结果和预期结果。
- 必要时附上错误截图，但请先移除密钥、账号和内部地址等敏感信息。

## 开发流程

1. Fork 仓库并从 `main` 创建分支。
2. 安装 Node.js、Rust stable、Visual Studio Build Tools 2022 和 Windows SDK。
3. 运行 `npm install` 安装依赖。
4. 完成修改，并为 Bug 修复补充回归测试。
5. 运行 `npm run check`，确保所有检查通过。
6. 提交 Pull Request，说明问题、修改方案和验证结果。

## 代码原则

- 自动修复不能静默丢失用户数据。
- 用户输入和解析器错误在插入 HTML 前必须转义。
- 新增 Tauri 命令时，需要同步注册并补充前后端连通性测试。
- 面向用户的主要界面和文档使用简体中文，必要时补充英文说明。
- 不提交构建目录、密钥、个人路径或包含敏感数据的测试文件。

## 提交信息

建议使用简洁明确的提交信息，例如：

```text
fix: 修复 YAML 重复键处理
feat: 增加新的格式检查能力
docs: 更新中文使用说明
test: 补充大型 Diff 回归测试
```

提交贡献即表示你同意按照项目的 [MIT License](LICENSE) 发布相关代码和文档。
